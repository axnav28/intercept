[project]
name = "intercept-backend"
version = "0.1.0"
description = "Backend services for the Intercept hackathon prototype"
requires-python = ">=3.11"

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"
