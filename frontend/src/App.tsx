export default function App() {
  return (
    <main className="flex min-h-screen items-center justify-center bg-slate-50 text-slate-900">
      <div className="rounded-3xl border border-slate-200 bg-white p-10 shadow-sm">
        <p className="text-sm uppercase tracking-[0.3em] text-emerald-600">Intercept</p>
        <h1 className="mt-3 text-3xl font-semibold">Dispatcher dashboard setup in progress</h1>
        <p className="mt-2 max-w-xl text-sm text-slate-600">
          Phase 1 establishes the monorepo, tooling, and app boundaries for the live emergency
          call prototype.
        </p>
      </div>
    </main>
  );
}
